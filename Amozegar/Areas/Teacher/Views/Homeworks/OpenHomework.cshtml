@model ChangeHomeworkStateViewModel
@{
    ViewData["Title"] = "باز کردن تکلیف";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container my-2">
    <div class="alert alert-warning">
        آیا از باز کردن تکلیف با عنوان @Model.HomeworkTitle اطمینان دارید؟
        <span class="text-danger" asp-validation-for="@Model.HomeworkTitle"></span>
        <form method="post" class="my-3">
            @Html.AntiForgeryToken()
            @Html.HiddenFor(n => n.HomeworkId)
            @Html.HiddenFor(n => n.HomeworkTitle)
            <a asp-action="Index"
               asp-controller="Homeworks"
               asp-area="Shared"
               asp-route-classId="@ViewBag.ClassId"
               asp-route-roleName="Teacher"
               asp-route-pageNumber="1"
               class="btn btn-warning">برگشت به تکالیف</a>
            <button type="submit" class="btn btn-danger">باز کردن</button>
        </form>
    </div>
</div>
