@model StudentsActionsViewModel
@{
    ViewData["Title"] = "بن کردن کاربر";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container my-2">
    <div class="alert alert-warning">
        آیا از بن کردن دانش آموز با نام @Model.StudentName اطمینان دارید؟
        <form method="post" class="my-3">
            @Html.AntiForgeryToken()
            @Html.HiddenFor(s => s.StudentInClassId)
            <button type="submit" class="btn btn-warning">بن کردن</button>
        </form>
    </div>
</div>
