@model AddOrEditNotificaionsViewModel
@{
    ViewData["Title"] = "افزودن اعلان";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="min-vh-100 d-flex align-items-center justify-content-center bg-light">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-10">
                <div class="card shadow-lg border-0">
                    <div class="card-body p-5">
                        <div class="text-center mb-5">
                            <h2 class="fw-bold">ایجاد اعلان در کلاس</h2>
                            <p class="text-muted">خبر ها را در اینحا به اشتراک بگزارید</p>
                        </div>

                        <form method="post" novalidate enctype="multipart/form-data">
                            @Html.AntiForgeryToken()
                            <div class="mb-4">
                                <label asp-for="@Model.Title" class="form-label"></label>
                                <div class="input-group">
                                    <span class="input-group-text bg-white">
                                        <i class="fa-solid fa-heading text-primary"></i>
                                    </span>
                                    <input asp-for="@Model.Title" class="form-control">
                                </div>
                                <span class="small text-danger" asp-validation-for="@Model.Title"></span>
                            </div>

                            <div class="mb-4">
                                <label asp-for="@Model.Body" class="form-label"></label>
                                <div class="input-group">
                                    <span class="input-group-text bg-white">
                                        <i class="fa-solid fa-file-lines text-primary"></i>
                                    </span>
                                    <textarea asp-for="@Model.Body" class="form-control"></textarea>
                                </div>
                                <span class="small text-danger" asp-validation-for="@Model.Body"></span>
                            </div>



                            <div class="mb-4">
                                <label asp-for="@Model.Pictures" class="form-label"></label>
                                <div class="input-group">
                                    <span class="input-group-text bg-white">
                                        <i class="fa-solid fa-image text-primary"></i>
                                    </span>
                                    <input type="file" name="Pictures" id="Pictures" multiple class="form-control">
                                </div>
                                <span class="small text-danger" asp-validation-for="@Model.Pictures"></span>
                            </div>



                            <div class="mb-4 d-flex ">
                                <a asp-action="Notifications"
                                   asp-controller="Home"
                                   asp-area="Teacher"
                                   asp-route-classId="@ViewBag.ClassId"
                                   asp-route-pageNumber="1"
                                   class="btn btn-warning w-50 py-3 d-block rounded-end-0">برگشت به اعلانات</a>
                                <button type="submit" class="btn btn-primary w-50 py-3 d-block rounded-start-0">
                                    <i class="fas fa-edit me-2"></i>
                                    ثبت
                                </button>
                            </div>


                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
